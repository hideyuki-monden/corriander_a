name: corriandar_a_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  cloud_firestore: ^1.0.5
  firebase_auth: ^1.1.0
  firebase_core: ^1.0.3
  image_picker: ^0.7.4
  firebase_storage: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
